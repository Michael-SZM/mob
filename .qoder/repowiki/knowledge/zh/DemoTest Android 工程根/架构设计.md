根目录仅承担 Gradle 工程编排职责：settings.gradle.kts 通过 dependencyResolutionManagement 统一配置 google、mavenCentral 与阿里云镜像仓库并禁止子项目自行声明仓库；build.gradle.kts 在顶层以 alias(libs.plugins.*) 方式声明 android.application 与 kotlin.android 插件（apply false），由 app 子模块按需引入；当前仅 include(':app') 一个 Android 应用子模块，所有构建配置收敛于根，子模块只关注业务实现。